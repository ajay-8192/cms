import PageHeader from "../../components/Common/PageHeader";

const BulkUpload = () => {
    return (
        <div className="h-screen w-full bg-gray-200 overflow-auto">
            <PageHeader header="Bulk Upload" />
        </div>
    );
};

export default BulkUpload;
